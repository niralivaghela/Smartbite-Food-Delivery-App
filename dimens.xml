<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ── Spacing ────────────────────────────────────────────────────────── -->
    <dimen name="spacing_xxs">2dp</dimen>
    <dimen name="spacing_xs">4dp</dimen>
    <dimen name="spacing_sm">8dp</dimen>
    <dimen name="spacing_md">16dp</dimen>
    <dimen name="spacing_lg">24dp</dimen>
    <dimen name="spacing_xl">32dp</dimen>
    <dimen name="spacing_xxl">48dp</dimen>

    <!-- ── Corner Radii ───────────────────────────────────────────────────── -->
    <dimen name="radius_xs">4dp</dimen>
    <dimen name="radius_sm">8dp</dimen>
    <dimen name="radius_md">12dp</dimen>
    <dimen name="radius_lg">16dp</dimen>
    <dimen name="radius_xl">24dp</dimen>
    <dimen name="radius_xxl">32dp</dimen>
    <dimen name="radius_round">50dp</dimen>

    <!-- Legacy aliases -->
    <dimen name="card_radius">16dp</dimen>
    <dimen name="button_radius">14dp</dimen>
    <dimen name="chip_radius">20dp</dimen>
    <dimen name="badge_radius">12dp</dimen>

    <!-- ── Elevation ──────────────────────────────────────────────────────── -->
    <dimen name="elevation_none">0dp</dimen>
    <dimen name="elevation_xs">1dp</dimen>
    <dimen name="elevation_sm">2dp</dimen>
    <dimen name="elevation_md">4dp</dimen>
    <dimen name="elevation_lg">8dp</dimen>
    <dimen name="elevation_xl">12dp</dimen>
    <dimen name="elevation_fab">16dp</dimen>

    <!-- ── Typography ─────────────────────────────────────────────────────── -->
    <dimen name="text_caption">11sp</dimen>
    <dimen name="text_small">12sp</dimen>
    <dimen name="text_body">14sp</dimen>
    <dimen name="text_subtitle">16sp</dimen>
    <dimen name="text_title">18sp</dimen>
    <dimen name="text_heading">22sp</dimen>
    <dimen name="text_display">28sp</dimen>
    <dimen name="text_hero">36sp</dimen>

    <!-- ── Component heights ──────────────────────────────────────────────── -->
    <dimen name="bottom_nav_height">64dp</dimen>
    <dimen name="toolbar_height">56dp</dimen>
    <dimen name="button_height">52dp</dimen>
    <dimen name="input_height">56dp</dimen>
    <dimen name="chip_height">36dp</dimen>
    <dimen name="fab_size">56dp</dimen>
    <dimen name="fab_mini_size">40dp</dimen>

    <!-- ── Image sizes ────────────────────────────────────────────────────── -->
    <dimen name="avatar_sm">36dp</dimen>
    <dimen name="avatar_md">48dp</dimen>
    <dimen name="avatar_lg">72dp</dimen>
    <dimen name="avatar_xl">96dp</dimen>
    <dimen name="restaurant_card_image">200dp</dimen>
    <dimen name="food_thumb">80dp</dimen>
    <dimen name="category_card_size">90dp</dimen>
    <dimen name="category_image_size">60dp</dimen>

    <!-- ── Card sizes ─────────────────────────────────────────────────────── -->
    <dimen name="restaurant_card_width">260dp</dimen>
    <dimen name="restaurant_card_small_width">150dp</dimen>
    <dimen name="cart_item_height">100dp</dimen>

    <!-- ── Icon sizes ─────────────────────────────────────────────────────── -->
    <dimen name="icon_xs">16dp</dimen>
    <dimen name="icon_sm">20dp</dimen>
    <dimen name="icon_md">24dp</dimen>
    <dimen name="icon_lg">32dp</dimen>
    <dimen name="icon_xl">48dp</dimen>

    <!-- ── OTP boxes ──────────────────────────────────────────────────────── -->
    <dimen name="otp_box_size">52dp</dimen>
    <dimen name="otp_box_radius">12dp</dimen>
    <dimen name="otp_text_size">22sp</dimen>

    <!-- ── Dashboard KPI tiles ───────────────────────────────────────────── -->
    <dimen name="kpi_tile_height">120dp</dimen>
    <dimen name="kpi_icon_size">40dp</dimen>
    <dimen name="kpi_value_size">26sp</dimen>

    <!-- ── Bottom sheet ───────────────────────────────────────────────────── -->
    <dimen name="bottom_sheet_peek">300dp</dimen>
    <dimen name="bottom_sheet_radius">24dp</dimen>

    <!-- ── Stroke widths ──────────────────────────────────────────────────── -->
    <dimen name="stroke_thin">1dp</dimen>
    <dimen name="stroke_medium">2dp</dimen>
    <dimen name="stroke_thick">3dp</dimen>

    <!-- ── Shimmer ────────────────────────────────────────────────────────── -->
    <dimen name="shimmer_item_height">120dp</dimen>

</resources>