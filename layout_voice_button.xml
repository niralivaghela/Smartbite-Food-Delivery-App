<?xml version="1.0" encoding="utf-8"?>
<!-- layout_voice_button - SmartBite: animated voice/mic FAB widget -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Pulse animation ring (shown while recording; animates via code) -->
    <View
        android:id="@+id/viewPulse"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_circle_primary"
        android:alpha="0.3"
        android:visibility="gone" />

    <!-- Mic FAB -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabVoice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:src="@drawable/ic_mic"
        android:contentDescription="@string/nav_search" />

    <!-- "Listening…" label shown while recording -->
    <TextView
        android:id="@+id/tvListening"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginTop="8dp"
        android:text="@string/loading"
        android:textColor="@color/primary"
        android:textSize="12sp"
        android:textStyle="bold"
        android:visibility="gone" />

</FrameLayout>